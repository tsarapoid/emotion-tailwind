git remote add origin https://github.com/tsarapoid/emotion-tailwind.git
git branch -M main
git push -u origin main

//yarn add gh-pages
npm i --save gh-pages

//npm cache clean --force
//npm install --save gh-pages
//npm install ajv

[add to package.json]
  "homepage": "https://tsarapoid.github.io/emotion-tailwind",
  "repository": {
    "type": "git",
    "url": "https://github.com/tsarapoid/emotion-tailwind.git"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
  },

git add .
git commit -m "CRA app with GH Pages"
git push origin master

npm run build
npm run deploy